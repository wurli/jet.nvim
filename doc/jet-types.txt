*jet-types.txt*	

                                jet.nvim types


                                       Type |gO| to see the table of contents.


Types defined by jet.nvim which extensions/ambitious users may want to use.

------------------------------------------------------------------------------
`Pos` and `Range`                                      *jet-types-pos-and-range*

*jet.send.Pos*

    TODO: swap for vim.Pos once it's stabilised

    Fields: ~
        • {buf}  (`integer`)
        • {row}  (`integer`) 0-indexed
        • {col}  (`integer`) 0-indexed

Pos.new({opts})                                            *jet-types-Pos.new()*

    Parameters: ~
        • {opts}      (`Partial<jet.send.Pos>`)

    Return: ~
        (`jet.send.Pos`)

Pos:eq({p})                                                 *jet-types-Pos:eq()*

    Check equality against another `Pos`

    Parameters: ~
        • {p}         (`jet.send.Pos`)

    Return: ~
        (`boolean`)

Pos:nudge({n})                                           *jet-types-Pos:nudge()*

    Move a Pos forward/backward by one byte

    Parameters: ~
        • {n}         (`(-1|1)?`)

    Return: ~
        (`jet.send.Pos?`)

Pos:lt({p})                                                 *jet-types-Pos:lt()*

    Check if a position is before another one

    Parameters: ~
        • {p}         (`jet.send.Pos`)

    Return: ~
        (`boolean`)

Pos.get_curr()                                        *jet-types-Pos.get_curr()*

    Get the cursor `Pos`

    Return: ~
        (`jet.send.Pos`)

Pos:to_char()                                          *jet-types-Pos:to_char()*

    Get the character at the `Pos`

    Return: ~
        (`string?`)

Pos:to_cursor()                                      *jet-types-Pos:to_cursor()*

    Get a row, col pair for use with `vim.fn.cursor()`

    Return: ~
        (`integer`)
        (`integer`)

Pos:lang_info()                                      *jet-types-Pos:lang_info()*

    Adapted from
    https://github.com/neovim/neovim/blob/master/runtime/lua/vim/_comment.lua
    NOTE: if this causes issues in the future (e.g. we don't actually want the
    range-specific filetype) we could instead return a table of candidate
    filetypes and commentstrings, then match these against kernel filetypes
    until we find a match. Don't do this until it's clear that it's a real
    issue.

    Return: ~
        (`{ commentstring: string?, filetype: string? }`)

*jet.send.Range*

    TODO: swap for vim.Range once it's stabilised

    Fields: ~
        • {buf}        (`integer`)
        • {start_row}  (`integer`) 0-indexed
        • {start_col}  (`integer`) 0-indexed
        • {end_row}    (`integer`) 0-indexed
        • {end_col}    (`integer`) 0-indexed

Range.new({opts})                                        *jet-types-Range.new()*

    Parameters: ~
        • {opts}      (`Partial<jet.send.Range>`)

    Return: ~
        (`jet.send.Range`)

Range:text()                                            *jet-types-Range:text()*

    Get the buffer text covered by the `Range`

    Return: ~
        (`string[]?`)

Range:code({opts})                                      *jet-types-Range:code()*

    Get the code covered by the `Range`.

    Also returns the local filetype at the range start.

    Parameters: ~
        • {opts}      (`jet.send.range_code.Opts?`)
                      • comments (`boolean?`): Set to `true` to include
                        comments in the returned code.

Range:start()                                          *jet-types-Range:start()*

    Get the `Range` start boundary as a |jet.Pos|

    Return: ~
        (`jet.send.Pos`)

Range:_end()                                            *jet-types-Range:_end()*

    Get the `Range` end boundary as a |jet.Pos|

    Return: ~
        (`jet.send.Pos`)

Range:textobject()                                *jet-types-Range:textobject()*

    Select the `Range` as a "text-object" (see |omap-info|)

------------------------------------------------------------------------------
Repl and image buffers                        *jet-types-repl-and-image-buffers*

*jet.Kernel.Term*

    Inherits from |jet.Buf|

    Fields: ~
        • {job_id}  (`integer`)

Term.init({opts})                                        *jet-types-Term.init()*

    Parameters: ~
        • {opts}      (`jet.Kernel.Term.init.Opts`)
                      • kernel (`jet.Kernel`)
                      • ns (`integer`)

    Return: ~
        (`jet.Kernel.Term`)

Term:send({code}, {tabstop})                             *jet-types-Term:send()*

    Parameters: ~
        • {code}      (`(string|string[])`) Code to be sent
        • {tabstop}   (`integer?`) Optional; number of spaces to use for tab
                      characters

*jet.Kernel.Img*

    Inherits from |jet.Buf|

    Fields: ~
        • {img_file}  (`string?`)

Img.init({opts})                                          *jet-types-Img.init()*

    Parameters: ~
        • {opts}      (`jet.Kernel.Img.init.Opts`)
                      • kernel (`jet.Kernel`)
                      • ns (`integer`)

    Return: ~
        (`jet.Kernel.Img`)

Img:open({focus}, {which})                                *jet-types-Img:open()*

    Parameters: ~
        • {focus}     (`boolean?`)
        • {which}     (`(string|integer)?`)

    Return: ~
        (`integer`) Win number

Img:list_files()                                    *jet-types-Img:list_files()*

    Return: ~
        (`string[]`)
        (`integer?`)

Img:display({which})                                   *jet-types-Img:display()*

    Parameters: ~
        • {which}     (`(string|integer)?`)

*jet.Buf*

    Fields: ~
        • {buf}        (`integer`)
        • {name}       (`string`)
        • {augroup}    (`integer`)
        • {ns}         (`integer`)
        • {kernel}     (`jet.Kernel`)
        • {open_opts}  (`(vim.api.keyset.win_config|fun() -> vim.api.keyset.win_config)`)

Buf.init({class}, {opts})                                 *jet-types-Buf.init()*

    Parameters: ~
        • {class}     (`T?`)
        • {opts}      (`jet.Buf.init.Opts`)
                      • name (`string`)
                      • ns (`integer`)
                      • kernel (`jet.Kernel`)
                      • open_opts
                        (`(vim.api.keyset.win_config|fun() -> vim.api.keyset.win_config)`)

    Return: ~
        (`T`)

Buf:win()                                                  *jet-types-Buf:win()*

    Get a window displaying the buffer, if there is one

    Return: ~
        (`integer?`)

Buf:open({focus}, {opts})                                 *jet-types-Buf:open()*

    Parameters: ~
        • {focus}     (`boolean?`)
        • {opts}      (`vim.api.keyset.win_config?`)

    Return: ~
        (`integer`) The opened window

Buf:toggle()                                            *jet-types-Buf:toggle()*

Buf:delete()                                            *jet-types-Buf:delete()*

Buf:create_autocmd({event}, {callback})         *jet-types-Buf:create_autocmd()*

    Parameters: ~
        • {event}     (`(vim.api.keyset.events|vim.api.keyset.events[])`)
        • {callback}  (`(string|fun(args: vim.api.keyset.create_autocmd.callback_args) -> boolean?)`)

 vim:tw=78:sw=4:ts=4:ft=help:norl:et:
